#!/usr/bin/env bash
set -euo pipefail

if [[ $# -ne 0 ]]; then
  echo "Usage: ${0##*/}" >&2
  exit 1
fi

while IFS= read -r line || [[ -n $line ]]; do
  echo "$line" | tr '[:upper:]' '[:lower:]' | sed 's/ /-/g; s/[^a-z0-9-]//g'
done
